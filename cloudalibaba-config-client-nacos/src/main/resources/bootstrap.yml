server:
  port: 8083
spring:
  application:
    name: config-client-nacos
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

service-url:
  nacos-payment-servcie: http://provider-payment-nacos
